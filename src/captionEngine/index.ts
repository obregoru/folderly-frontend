// Remotion entry point. Loaded by Remotion Studio (`npm run remotion`)
// and by the programmatic bundler in the backend render endpoint.
import { registerRoot } from 'remotion';
import { RemotionRoot } from './Root';

registerRoot(RemotionRoot);
